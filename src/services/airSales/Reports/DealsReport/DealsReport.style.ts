export const styles = {
  downloadIcon: (theme: any) => {
    return {
      border: `1px solid ${theme?.palette?.grey[700]}`,
      width: '52px',
      height: '52px',
      display: 'flex',
      justifyContent: 'center',
      alignItems: 'center',
      borderRadius: '8px',
      cursor: 'pointer',
    };
  },
};
