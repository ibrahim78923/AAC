export const styles = {
  emailWrapper: {
    display: 'flex',
    justifyContent: 'space-between',
  },
  heading: (theme: any) => ({
    color: theme.palette.grey[800],
  }),
  paragraph: (theme: any) => ({
    color: theme.palette.custom.main,
    paddingBottom: '20px',
  }),
};
