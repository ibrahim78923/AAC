export const style = {
  stepperIconColor: (borderColor: any) => {
    return {
      border: `1px solid ${borderColor}`,
      borderRadius: '50%',
      width: 40,
      height: 40,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
    };
  },
};
