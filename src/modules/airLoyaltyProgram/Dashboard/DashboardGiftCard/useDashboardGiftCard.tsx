import { giftCardsColumns, giftCardsData } from './DashboardGiftCard.data';

const useDashboardGiftCard = () => {
  const contentHeight = 310;

  return {
    giftCardsColumns,
    giftCardsData,
    contentHeight,
  };
};

export default useDashboardGiftCard;
