import { Box } from '@mui/material';
import { styles } from './QuoteCreatedFor.style';

const QuoteCreatedFor = () => {
  return <Box sx={styles?.card}>This quote is created for pc products.</Box>;
};

export default QuoteCreatedFor;
