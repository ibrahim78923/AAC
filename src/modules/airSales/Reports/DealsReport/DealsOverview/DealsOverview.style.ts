export const styles = {
  dealBox: (theme: any) => {
    return {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'center',
      border: `1px solid ${theme?.palette?.custom.off_white_three}`,
      padding: '24px',
    };
  },
};
