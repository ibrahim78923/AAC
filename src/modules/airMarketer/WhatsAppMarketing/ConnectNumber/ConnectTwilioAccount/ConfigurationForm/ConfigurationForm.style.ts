export const styles = {
  configurationBox: (theme: any) => ({
    border: `1px solid ${theme?.palette?.grey[700]}`,
    borderRadius: '10px',
    padding: '20px',
    width: '100%',
    mt: 4,
  }),
};
