import { Box } from '@mui/material';
import { styles } from '../../EngagementAd.style';

const Automation = () => {
  return <Box sx={styles?.engagementStyle}>Automation</Box>;
};

export default Automation;
