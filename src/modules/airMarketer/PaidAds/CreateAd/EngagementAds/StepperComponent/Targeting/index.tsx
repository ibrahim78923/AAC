import { styles } from '../../EngagementAd.style';
import { Box } from '@mui/material';

const Targeting = () => {
  return <Box sx={styles?.engagementStyle}>Targeting</Box>;
};

export default Targeting;
