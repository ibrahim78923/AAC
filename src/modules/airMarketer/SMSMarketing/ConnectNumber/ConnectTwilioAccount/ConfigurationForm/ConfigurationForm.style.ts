export const styles = {
  configurationBox: (theme: any) => ({
    border: `1px solid ${theme?.palette?.custom?.hex_grey}`,
    borderRadius: '10px',
    padding: '20px',
    width: '100%',
    mt: 4,
  }),
};
